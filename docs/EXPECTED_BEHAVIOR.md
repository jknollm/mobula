# Expected Behavior

This document is the lightweight reference for how the app is expected to behave from a user point of view.

Use it to record feedback-driven expectations that should remain stable over time. Keep entries concrete, observable, and written in product language rather than implementation language.

When behavior changes, update this file so there is a current reference the team can compare the real app against.
